import { Typography, Box } from '@vegangouda/web/design-system';

interface UserDashProps {}

export const UserDash = ({}: UserDashProps) => {
  return (
    <Box>
      <Typography>User Dashboard</Typography>
      <Typography>TODO</Typography>
    </Box>
  );
};
